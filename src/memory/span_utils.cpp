// Span utilities (header-only; this file for future extensions)
#include "streamcipher/memory/span_utils.hpp"

namespace streamcipher::memory {
    // All utilities are constexpr/inline in the header.
}
