// Memory pool implementation (template is header-only; this file exists for future extensions)
#include "streamcipher/memory/pool.hpp"

namespace streamcipher::memory {
    // Explicit template instantiations go here when needed.
    // The SlabPool template is header-only for maximum inlining.
}
